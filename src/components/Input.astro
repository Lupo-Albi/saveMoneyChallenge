---
type HTMLInputTypeAttribute =
  | "number"
  | "button"
  | "checkbox"
  | "color"
  | "date"
  | "datetime-local"
  | "email"
  | "file"
  | "hidden"
  | "image"
  | "month"
  | "password"
  | "radio"
  | "range"
  | "reset"
  | "search"
  | "submit"
  | "tel"
  | "text"
  | "time"
  | "url"
  | "week";

interface Props {
  title: string;
  type: HTMLInputTypeAttribute;
}

const { title, type } = Astro.props;
---

<input name={title} type={type} />
